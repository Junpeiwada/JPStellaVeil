# 実装計画-JPStellaVeil

## 変更履歴

| 日付 | 変更者 | 変更内容 |
| --- | --- | --- |
| 2026-08-24 | Junpei Wada | 初版作成。Phase 0〜7 でフェーズ分け |
| 2026-08-24 | Junpei Wada | Phase 1 を完了。色管理方針（中間 linearSRGB 固定、最終出力で入力 ICC へ復元）と ICC 比較キー（`desc` タグ由来の記述名）を確定 |
| 2026-08-24 | Junpei Wada | Phase 2 / Phase 3 を完了。MetalKit キャンバスとレイヤー管理 UI を実装 |
| 2026-08-24 | Junpei Wada | Phase 4 の方針を確定（縮小プレビューを行わない、適用ボタン方式、タイル分割によるキャンセル可能処理）。`引き継ぎ.md` を追加 |
| 2026-08-24 | Junpei Wada | Phase 4 を完了。タイル分割グロー処理・適用ボタン・キャンセル・最終書き出し接続を実装。スプリット比較の既定値の誤り（実装と逆で処理結果が表示されない）を修正 |
| 2026-08-24 | Junpei Wada | impl-plan 形式へ全面書き換え。技術仕様を [技術仕様.md](技術仕様.md) へ分離。旧 Phase 0 をフェーズ1へ統合（旧 Phase 2 以降の番号は変更なし）。タスクIDを `P<N>-<連番>` 形式に付与 |
| 2026-08-24 | Junpei Wada | フェーズ4に P4-9〜P4-12 を追加。適用ボタン方式から自動処理へ変更（実測 0.2 秒で十分速いため）。明るさ応答・グローのみ表示・ドラッグ＆ドロップを追加 |
| 2026-08-24 | Junpei Wada | フェーズ4に P4-13〜P4-15 を追加。ドラッグでパンできない不具合を修正し元画像比較をスペースキーへ移動。タイル寸法とコマンドバッファのまとめ方を見直して 0.2 秒 → 0.16 秒。表示状態を分離して描画レスポンスを改善 |

## 目次

- [概要](#概要)
- [進捗](#進捗)
  - [更新のきまり](#更新のきまり)
- [フェーズ1: 土台構築と TIFF 入出力・色管理](#フェーズ1-土台構築と-tiff-入出力色管理)
  - [タスク](#タスク)
  - [完了確認](#完了確認)
  - [フェーズ7へ持ち越した残作業](#フェーズ7へ持ち越した残作業)
- [フェーズ2: キャンバス表示と操作](#フェーズ2-キャンバス表示と操作)
  - [タスク](#タスク-1)
  - [完了確認](#完了確認-1)
- [フェーズ3: レイヤー管理 UI](#フェーズ3-レイヤー管理-ui)
  - [タスク](#タスク-2)
  - [完了確認](#完了確認-2)
- [フェーズ4: 画像処理パイプライン](#フェーズ4-画像処理パイプライン)
  - [タスク](#タスク-3)
  - [完了確認](#完了確認-3)
  - [フェーズ5へ持ち越した残作業](#フェーズ5へ持ち越した残作業)
- [フェーズ5: 空マスク（自動 + 手動補正）](#フェーズ5-空マスク自動--手動補正)
  - [タスク](#タスク-4)
  - [完了確認](#完了確認-4)
- [フェーズ6: プロジェクト保存（.stellaveil）](#フェーズ6-プロジェクト保存stellaveil)
  - [タスク](#タスク-5)
  - [完了確認](#完了確認-5)
- [フェーズ7: メタデータ検証付き書き出し](#フェーズ7-メタデータ検証付き書き出し)
  - [タスク](#タスク-6)
  - [完了確認](#完了確認-6)
- [全体の完了基準](#全体の完了基準)
- [注意点](#注意点)

---

## 概要

16bit RGB TIFF に星空向けのグローを複数レイヤーで非破壊的に加える macOS ネイティブアプリを実装する。
前景の人工光を不用意ににじませないよう空マスクと手動補正を備え、書き出し時にはメタデータ保持を自動検証して、検証に失敗した出力は破棄する。

- **対象**: 16bit TIFF の入力 → グローレイヤー編集 → 空マスク適用 → メタデータ検証付き 16bit TIFF 出力
- **やらないこと**: RAW 現像 / JPEG 出力 / 汎用レタッチフィルター / 複数画像のスタック合成
- **関連**: [技術仕様.md](技術仕様.md)（確定仕様の正本） / [アプリ全体像.md](アプリ全体像.md) / [UI.md](UI.md) / [引き継ぎ.md](引き継ぎ.md)（別マシンでの再開手順）

## 進捗

**現在**: フェーズ5（次の一手: P5-1 自動空マスクの生成方式を決める）

| フェーズ | 状態 | 完了 | 備考 |
| --- | --- | --- | --- |
| 1. 土台構築と TIFF 入出力・色管理 | ✅ 完了 | 13/13 | ExifTool 同梱はフェーズ7へ持ち越し |
| 2. キャンバス表示と操作 | ✅ 完了 | 6/6 | — |
| 3. レイヤー管理 UI | ✅ 完了 | 6/6 | サイドバー幅の可変化はフェーズ4で対応 |
| 4. 画像処理パイプライン | ✅ 完了 | 15/15 | 8640 x 4860 を 15 タイル / 約 0.16 秒（M4 Pro）。自動処理へ変更 |
| 5. 空マスク（自動 + 手動補正） | 🔄 進行中 | 0/8 | 自動生成の方式が未決定（P5-1） |
| 6. プロジェクト保存（.stellaveil） | ⬜ 未着手 | 0/4 | — |
| 7. メタデータ検証付き書き出し | ⬜ 未着手 | 0/4 | — |

> 状態: ⬜ 未着手 / 🔄 進行中 / ✅ 完了 / ⏸️ 保留。タスクは `- [ ]` / `- [x]`。
> フェーズ内の全タスクが `- [x]` になったら ✅ にする。備考は1行まで（詳細は各フェーズ本文へ）。

### 更新のきまり

- 更新はフェーズ完了時。実装したターン内で書き換える（「動いた＝更新済み」と錯覚しない）
- **状態はこの表だけに書く**。フェーズ見出しには書かない
- 計画とズレたら計画側を直し、変更履歴に残す
- 各フェーズの完了時はアプリを起動してスクリーンショットで確認し、それからコミットする

---

## フェーズ1: 土台構築と TIFF 入出力・色管理

**目標**: 16bit TIFF を読み込んで ICC とメタデータを保ったまま書き出し、その保持を自動検証できる。

### タスク

- [x] **P1-1**: XcodeGen によるプロジェクト構成を作成（`project.yml`）
- [x] **P1-2**: ドメインモデル初版（Project / GlowLayer / MaskState / ExportJob）を定義（`Sources/Domain/StellaVeilProject.swift`）
- [x] **P1-3**: 最小 UI とアプリ状態管理を実装（`Sources/App/JPStellaVeilApp.swift`、`Sources/App/AppState.swift`）
- [x] **P1-4**: 単体テスト基盤とエラー型（ユーザー向け文言 + 詳細ログ）を追加（`Tests/JPStellaVeilTests/`）
- [x] **P1-5**: 16bit TIFF の読み込みと基本プロパティ検査（`Sources/Imaging/TIFFImageIOService.swift`）
- [x] **P1-6**: 入力 TIFF 台帳（寸法・ビット深度・色情報・SHA256・メタデータ件数）を保持（`Sources/Imaging/TIFFImageIOService.swift`）
- [x] **P1-7**: リニア化処理（linearSRGB）を実装（`Sources/Imaging/TIFFImageIOService.swift`）
- [x] **P1-8**: 中間 TIFF の書き出しと属性互換チェック（`Sources/Imaging/TIFFImageIOService.swift`）
- [x] **P1-9**: Save Panel 経由の中間 TIFF 書き出し導線（`Sources/App/ContentView.swift`）
- [x] **P1-10**: ICC プロファイル一致の厳密検証（`Sources/Imaging/ColorProfileInspector.swift`。仕様は[技術仕様 §2](技術仕様.md#2-色管理仕様)）
- [x] **P1-11**: メタデータ比較の除外規則と値正規化を定義（`Sources/Metadata/MetadataComparisonPolicy.swift`。仕様は[技術仕様 §3](技術仕様.md#3-メタデータ比較仕様)）
- [x] **P1-12**: ExifTool 実行・メタデータコピー・比較判定を実装（`Sources/Metadata/ExifToolRunner.swift`、`Sources/Metadata/MetadataVerificationService.swift`）
- [x] **P1-13**: 最終出力（入力 ICC 復元）の書き出しと検証を実装（`Sources/Imaging/TIFFImageIOService.swift`）

### 完了確認

- [x] 16bit TIFF を読み込め、書き出した TIFF の寸法・ビット深度・ICC が期待どおり
- [x] 指定テストデータで最終書き出しの ICC 一致往復テストが通る
- [x] メタデータコピー後の比較検証が成功し、意図的に加えた差分は検出される

### フェーズ7へ持ち越した残作業

- ExifTool の同梱と署名運用（現状はシステムインストール版を探索して使用）→ P7-1
- 処理済み画像を通る書き出しパイプライン本体との接続 → P7-2

---

## フェーズ2: キャンバス表示と操作

**目標**: 16bit リニア RGB のまま画像を表示し、パン・ズーム・元画像比較が実時間で操作できる。

### タスク

- [x] **P2-1**: MetalKit キャンバス（Fit / 50% / 100% / 200%）を実装（`Sources/Rendering/CanvasView.swift`、`Sources/Rendering/CanvasRenderer.swift`）
- [x] **P2-2**: パン（ドラッグ、可動域の丸め）とズーム（Command+スクロール、ピンチ、上下限）（`Sources/Rendering/CanvasViewState.swift`）
- [x] **P2-3**: 押下中の元画像比較と Option+ドラッグのスプリット比較（`Sources/Rendering/CanvasView.swift`、`Sources/Rendering/CanvasShaders.metal`）
- [x] **P2-4**: マスクオーバーレイの表示切替（`Sources/Rendering/CanvasRenderer.swift`）
- [x] **P2-5**: 16bit リニア RGB テクスチャの読み込み（8bit 落ちとガンマ混入を回避）（`Sources/Rendering/MetalTextureLoader.swift`）
- [x] **P2-6**: 表示専用の露出調整（書き出しには影響しない）（`Sources/Rendering/CanvasShaders.metal`）

### 完了確認

- [x] プレビュー操作が実時間で追従する
- [x] 元画像比較がワンタッチで行える
- [x] Fit 倍率算出・中央寄せ・パン可動域・ズーム上下限・ビュー寸法未確定時の破綻回避がテストで担保されている

---

## フェーズ3: レイヤー管理 UI

**目標**: グローレイヤーの構成とパラメータを UI で一通り編集できる。

### タスク

- [x] **P3-1**: レイヤー追加（3プリセット）・複製・削除・表示/非表示・順序変更・名称変更（`Sources/App/LayerPanel.swift`、`Sources/App/AppState.swift`）
- [x] **P3-2**: 合成モード（Screen / Add）と不透明度の編集（`Sources/App/LayerPanel.swift`）
- [x] **P3-3**: インスペクタ（強度・半径・背景除去・ノイズ閾値・空マスク関連）（`Sources/App/LayerPanel.swift`）
- [x] **P3-4**: 各パラメータにスライダ・数値表示・既定値へ戻すボタンを付与（`Sources/App/LayerPanel.swift`）
- [x] **P3-5**: 半径が推奨上限を超えたときの霞み警告（`Sources/App/LayerPanel.swift`）
- [x] **P3-6**: ベース画像を最下段にロック表示（`Sources/App/LayerPanel.swift`）

### 完了確認

- [x] レイヤー構成が UI で一通り編集できる
- [x] 値変更でプレビュー更新のフラグが立つ
- [x] 複製時のパラメータ/マスク引き継ぎと ID 再発行、選択遷移がテストで担保されている

---

## フェーズ4: 画像処理パイプライン

**目標**: フル解像度のままグローを生成・合成し、その結果をそのまま最終 TIFF へ書き出せる。

### タスク

- [x] **P4-1**: マージン付きタイル分割の処理計画を実装（`Sources/Processing/GlowProcessingPlan.swift`）
- [x] **P4-2**: 背景減算による星成分抽出と閾値によるノイズ抑制（`Sources/Processing/GlowShaders.metal`）
- [x] **P4-3**: 4 成分ガウシアン PSF でグロー生成（分離可能フィルタ、横 → 縦）（`Sources/Processing/GlowPipeline.swift`）
- [x] **P4-4**: Screen / Add でのレイヤー合成（不透明度をゲインへ畳み込む）（`Sources/Processing/GlowShaders.metal`）
- [x] **P4-5**: 「適用」ボタン方式と未適用状態の表示（`Sources/App/ContentView.swift`、`Sources/App/AppState.swift`）
  - P4-9 で自動処理へ変更した。適用ボタンは中止したあとの再開用として残している
- [x] **P4-6**: タイル単位のキャンセル・進捗表示・焼き上がったタイルからの段階表示（`Sources/Processing/GlowProcessingController.swift`）
- [x] **P4-7**: 処理結果を最終 TIFF 書き出しへ接続（`Sources/App/ContentView.swift`、`Sources/Imaging/TIFFImageIOService.swift`）
- [x] **P4-8**: サイドバー幅の可変化（フェーズ3からの持ち越し。`HSplitView` へ変更）（`Sources/App/ContentView.swift`）
- [x] **P4-9**: 自動処理（150ms デバウンス、実行中の古い処理はキャンセル）（`Sources/App/AppState.swift`）
- [x] **P4-10**: 明るさ応答（成分ごとの明るさしきい値で PSF の形を明るさによって変える）
  （`Sources/Domain/StellaVeilProject.swift`、`Sources/Processing/GlowProcessingPlan.swift`、`Sources/Processing/GlowShaders.metal`）
- [x] **P4-11**: グローのみ表示（`Sources/Processing/GlowPipeline.swift`、`Sources/App/ContentView.swift`）
- [x] **P4-12**: TIFF のドラッグ＆ドロップ読み込み（`Sources/App/ContentView.swift`）
- [x] **P4-13**: 操作の割り当てを変更（ドラッグ＝パン、元画像比較＝スペースキー押下中）（`Sources/Rendering/CanvasView.swift`）
  - `mouseDown` で必ず比較へ入るため、ドラッグしてもパンできない状態だった
- [x] **P4-14**: 処理の高速化（タイル寸法の下限を 2048 へ、複数タイルを 1 コマンドバッファへまとめる）
  （`Sources/Processing/GlowProcessingPlan.swift`、`Sources/Processing/GlowPipeline.swift`）
  - 45 タイル / 約 0.2 秒 → 15 タイル / 約 0.16 秒
- [x] **P4-15**: 表示状態を `AppState` から分離し、パンのたびに画面全体が再評価されないようにした
  （`Sources/Rendering/CanvasDisplayState.swift`）

### 完了確認

- [x] プレビュー結果と書き出し結果の見え方の差異が許容範囲内
  - プレビューと書き出しは同一テクスチャを使うため原理的に一致する。テクスチャ → CGImage → TIFF → 読み戻しの往復で画素一致を検証済み
- [x] ガンマ空間のぼかしが混入しない（処理は linearSRGB のまま）
- [x] 指定テストデータ（8640 x 4860）のフル解像度処理が 45 タイル / 約 0.2 秒（Apple M4 Pro）
- [x] 星の明るさでハローの大きさが変わる（暗い星は芯だけ、明るい星は裾まで）
  - 明るさ応答を上げたとき、暗い星の方が強く削られることを自動テストで検証
- [x] レイヤーの表示切替やスライダー操作で自動的に再処理される
- [x] キャンバスをドラッグして表示位置を動かせる

### フェーズ5へ持ち越した残作業

- 空マスクの接続（現状は全面適用。シェーダ側の接続点は用意済み）→ P5-5
- グローの効き具合の調整（実写では控えめ。強度スライダーの範囲見直しを含む）→ P5-7
  - P4-10 の明るさ応答で「明るい星ほど大きなハロー」にはなった。全体の強さは未調整
- スプリット比較の境界がテクスチャ座標基準のため拡大表示で画面位置とずれる → P5-8

---

## フェーズ5: 空マスク（自動 + 手動補正）

**目標**: 前景の人工光へグローが乗るのをレイヤーごとに抑制できる。

指定テストデータでは、標準グロー適用時の平均輝度増分が空 0.00054 に対し前景（ゲル群の照明）0.00165 で、**前景の方が 3 倍強く反応している**。これがマスクで抑えたい現象そのもの。

### タスク

- [ ] **P5-1**: 自動空マスクの生成方式を決める（輝度しきい値 / 地平線より上 / 彩度 / 勾配。着手前に `AskUserQuestion` で確認する）
- [ ] **P5-2**: 自動空マスク生成を実装（新規 `Sources/Processing/SkyMaskGenerator.swift`）
- [ ] **P5-3**: 地平線指定 UI と境界ぼかし（`Sources/App/LayerPanel.swift`、`Sources/Rendering/CanvasView.swift`）
- [ ] **P5-4**: 手動ブラシ（加算/除外、ブラシ径・硬さ）（新規 `Sources/Rendering/MaskBrushController.swift`）
- [ ] **P5-5**: レイヤーごとのマスクをタイル処理へ接続（マスクもタイル座標へ切り出して読む）（`Sources/Processing/GlowPipeline.swift`、`Sources/Processing/GlowShaders.metal`）
- [ ] **P5-6**: マスク生成・ブラシ編集のテスト（新規 `Tests/JPStellaVeilTests/SkyMaskTests.swift`）
- [ ] **P5-7**: グローの効き具合を実写で調整（強度スライダー上限 5.0 とゲインの掛け方。**ユーザーの判断を仰ぐ**）（`Sources/App/LayerPanel.swift`、`Sources/Processing/GlowPipeline.swift`）
- [ ] **P5-8**: スプリット比較の境界ずれを修正（`CanvasViewState.imageRect` で画面座標 → テクスチャ座標へ変換）（`Sources/Rendering/CanvasView.swift`）

### 完了確認

- [ ] 指定テストデータで前景の輝度増分が空の増分を上回らなくなる
- [ ] レイヤーごとに独立した適用領域を編集でき、複製時に引き継がれる
- [ ] 拡大表示でもスプリット比較の境界がマウス位置と一致する

---

## フェーズ6: プロジェクト保存（.stellaveil）

**目標**: 編集状態を保存し、再読込で同じ状態へ戻せる。

### タスク

- [ ] **P6-1**: `.stellaveil` の Codable スキーマを定義（入力ファイルパス・ハッシュ・レイヤー・マスク・色管理情報）（`Sources/Domain/StellaVeilProject.swift`）
- [ ] **P6-2**: 保存と読込を実装（新規 `Sources/Persistence/ProjectDocumentService.swift`）
- [ ] **P6-3**: 入力ファイルのハッシュ不一致検知と再リンク導線（`Sources/App/ContentView.swift`）
- [ ] **P6-4**: エンコード/デコード往復と再読込一致のテスト（`Tests/JPStellaVeilTests/StellaVeilProjectTests.swift`）

### 完了確認

- [ ] 保存 → 再読込で編集状態が再現される
- [ ] 入力ファイルを差し替えた状態で開くと警告と再リンク導線が出る

---

## フェーズ7: メタデータ検証付き書き出し

**目標**: メタデータ検証に成功した出力だけが確定し、失敗時は原因が分かる。

フェーズ1でコピーと比較の実装自体は済んでいる。このフェーズは同梱・署名運用と、処理済み画像を通る本番の書き出し経路への接続が主題。

### タスク

- [ ] **P7-1**: ExifTool を同梱し、署名・公証を通す運用にする（`project.yml`、`Sources/Metadata/ExifToolRunner.swift`）
- [ ] **P7-2**: 処理済み画像の書き出し経路へメタデータコピーと比較検証を接続（`Sources/App/ContentView.swift`、`Sources/Metadata/MetadataVerificationService.swift`）
- [ ] **P7-3**: 検証失敗時に保存を中止し、失敗タグを表示する UI（`Sources/App/ContentView.swift`）
- [ ] **P7-4**: 読み込み → 調整 → 書き出し → 検証の結合テスト（`Tests/JPStellaVeilTests/GlowExportIntegrationTests.swift`）

### 完了確認

- [ ] メタデータ検証に成功したときのみ最終出力が確定する
- [ ] 失敗時に検証に失敗したタグが画面に表示される
- [ ] サンドボックス環境でも同梱 ExifTool が起動する

---

## 全体の完了基準

- [ ] 16bit TIFF の入力と 16bit TIFF 出力が安定動作する
- [ ] グローレイヤー編集とプレビュー反映が実用速度で動作する
- [ ] 空マスク（自動 / 手動）で前景漏れを抑制できる
- [ ] `.stellaveil` の保存 / 再読込が再現性を満たす
- [ ] メタデータ比較検証に成功した出力のみが保存される
- [ ] 指定テストデータで一連フロー（読み込み → 調整 → 書き出し → 検証）を完走できる
- [ ] 自動テストが全件成功する（2026-08-24 時点で 108 件）

## 注意点

- **リニア空間を外れない**: 抽出・ぼかし・合成は必ず linearSRGB で行う。ガンマ空間のぼかしが混入すると星のにじみ形状が変わる
- **縮小プレビューを入れない**: 見た目が変わるため。処理が重くなる分はタイル単位の段階表示とキャンセルで受ける（[技術仕様 §4.1](技術仕様.md#41-プレビューは縮小しない)）
- **テストデータのパスが直書き**: `TIFFColorManagementTests.swift`、`TIFFImageIOServiceTests.swift`、`GlowRealImageTests.swift` にフルパスがある。別マシンでは環境変数から読む形へ変えるとよい
- **キャンセル粒度はタイル単位**: `MTLCommandBuffer` は投入後に停止できない
