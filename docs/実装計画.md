# JPStellaVeil 実装計画

## 0. 進捗管理

### 0.1 進捗サマリ（2026-08-24 更新）
- 全体進捗: 62%
- 現在フェーズ: Phase 4（画像処理パイプライン）完了 → Phase 5 着手前
- 直近の到達点:
  - XcodeGen ベースのプロジェクト土台を作成
  - 16bit TIFF 読み込み/検査/中間書き出しの基盤を実装
  - 入力 TIFF 台帳（寸法、ビット深度、色情報、メタデータ件数、SHA256）を保持
  - Save Panel 経由の中間 TIFF 書き出し導線を実装
  - ICC プロファイル一致の厳密検証を実装（ICC `desc` タグ由来の記述名で比較）
  - 中間出力（linearSRGB 固定）と最終出力（入力 ICC 復元）の 2 系統に分離
  - ExifTool 連携（実行、JSON 読み取り、メタデータコピー、正規化比較検証）を実装
  - 検証失敗時に出力を破棄する導線を実装
  - MetalKit キャンバス（Fit/50%/100%/200%、パン、ズーム、押下中比較、
    Option+ドラッグのスプリット比較、マスクオーバーレイ、表示専用露出）を実装
  - レイヤー管理 UI（追加/複製/削除/表示切替/並べ替え/名称変更）とインスペクタを実装
  - タイル分割によるフル解像度グロー処理（背景減算 → 星抽出 → 4 成分 PSF → Screen/Add 合成）を実装
  - 「適用」ボタン方式、タイル単位のキャンセル、進捗表示、段階表示を実装
  - 処理結果を最終 TIFF 書き出しへ接続（プレビューと同じ画素を書き出す）
  - キャンバスとレイヤーパネルの境界をドラッグで変更できるようにした（Phase 3 の残課題）
  - スプリット比較の既定値が実装と逆で処理結果が表示されない不具合を修正
  - 指定テストデータ（8640 x 4860）のフル解像度処理が 45 タイル / 約 0.2 秒（Apple M4 Pro）
  - 自動テスト 101 件成功（うち指定テストデータでの ICC 一致往復とフル解像度処理を含む）

### 0.2 フェーズ進捗ボード
- [x] Phase 0: 土台構築
- [x] Phase 1: TIFF 入出力と色管理
- [x] Phase 2: キャンバス表示と操作
- [x] Phase 3: レイヤー管理 UI
- [x] Phase 4: 画像処理パイプライン
- [ ] Phase 5: 空マスク（自動 + 手動補正）（次に着手）
- [ ] Phase 6: プロジェクト保存（.stellaveil）
- [ ] Phase 7: メタデータ検証付き書き出し

### 0.3 更新履歴
- 2026-08-24:
  - Phase 0 を完了に更新
  - Phase 1 を進行中に更新
  - 進捗管理セクション（サマリ、ボード、履歴）を追加
  - Phase 1 を完了に更新（ICC 厳密検証と ExifTool 連携を実装）
  - 色管理方針を決定: 中間は linearSRGB 固定、最終出力で入力 ICC へ復元
  - ICC 比較キーを決定: ICC `desc` タグ由来のプロファイル記述名の正規化文字列
  - メタデータ比較の除外タグと値正規化規則を確定（4.1 / 4.2 節に記載）
  - Phase 2 を完了に更新（MetalKit キャンバスと操作）
  - Phase 3 を完了に更新（レイヤー管理 UI とインスペクタ）
  - Phase 4 の方針を確定: プレビュー縮小を行わず常にフル解像度、適用ボタン方式、
    タイル分割によるキャンセル可能・非ブロッキング処理（4.3 節に記載）
  - 引き継ぎ.md を追加（別マシンでの作業再開手順とハマりどころ）
  - Phase 4 を完了に更新（タイル分割グロー処理、適用ボタン、キャンセル、書き出し接続）
  - 4 成分 PSF の係数とタイル設計を 4.3.5 節に記載
  - Phase 3 の残課題だったサイドバー幅の可変化を Phase 4 で対応
  - スプリット比較の既定値を修正（0.0 が全面処理結果。従来値では処理結果が表示されなかった）

## 1. 目的
- 16bit RGB TIFF に対して、星空向けグローを複数レイヤーで非破壊編集できる macOS ネイティブアプリを実装する。
- 前景の人工光を不用意ににじませないため、空マスクと手動補正を実装する。
- 書き出し時にメタデータ保持を自動検証し、検証失敗時は出力失敗として扱う。

## 2. 第1版スコープ（実装対象）
- 16bit TIFF の入力、プレビュー、16bit TIFF 出力
- グローレイヤー（複数、表示/非表示、順序変更、複製、削除）
- 合成モード: Screen / Add（リニア空間）
- 星抽出（背景減算 + 閾値）
- 空マスク（自動 + 地平線指定 + 境界ぼかし + 手動補正）
- プロジェクト保存/再読込（.stellaveil）
- メタデータコピー + 入出力比較検証（ExifTool）

## 3. 非対象（第1版でやらない）
- RAW 現像
- JPEG 出力
- 汎用レタッチフィルター
- 複数画像スタック合成

## 4. アーキテクチャ実装方針
- UI層: SwiftUI
- 描画層: MetalKit（キャンバス、ズーム、比較表示、マスクオーバーレイ）
- 演算層: Metal Compute（星抽出、PSF 畳み込み、合成、マスク適用）
- IO層: ImageIO / ColorSync（16bit TIFF + ICC 維持）
- メタデータ層: 同梱 ExifTool（コピー + JSON 比較検証）
- 永続化層: Codable JSON（.stellaveil）

実装上の重要原則:
- 抽出、ぼかし、合成は必ずリニア RGB で実行する。
- プレビューは低解像度高速更新、書き出しはフル解像度処理に分離する。
- プレビュー結果と最終出力結果の処理契約を一致させる。

## 4.1 色管理仕様（Phase 1 で確定）

### 4.1.1 ICC の扱い
- 処理はリニア RGB（`linearSRGB`）で行う。
- 中間 TIFF: `linearSRGB` を埋め込む。入力プロファイルとは一致しないのが正常。
- 最終 TIFF: 処理後に入力 TIFF の ICC プロファイルへ変換して埋め込む。
- 検証は期待値を明示して行う（`ExpectedOutputProfile`）。
  - `.workingSpace(.linearSRGB)`: 中間出力に使用
  - `.matchesInput`: 最終出力に使用

### 4.1.2 ICC 一致判定の比較キー
- 比較キーはプロファイル記述名の正規化文字列。
- プロファイル名は ICC プロファイルの `desc` タグから取得する。
  - ICC v2 は `desc`（textDescriptionType）、v4 は `mluc`（multiLocalizedUnicodeType）に対応。
  - `CGColorSpace.name` はシステム識別子（`kCGColorSpaceLinearSRGB`）を返し、
    ImageIO の `kCGImagePropertyProfileName` は記述名（`sRGB IEC61966-2.1 Linear`）を返すため、
    両者を突き合わせるには `desc` タグ経由に統一する必要がある。
- 正規化規則: Unicode 互換正規化 → 小文字化 → 英数字以外を除去。
  - 例: `Adobe RGB (1998)` と `adobe-rgb-1998` は同値。
- どちらかがプロファイル名不明の場合は不一致として扱う（保存を止める側に倒す）。
- TIFF の `Model` タグ（カメラ機種名）をプロファイル名の代用にはしない。

## 4.2 メタデータ比較仕様（Phase 1 で確定）

### 4.2.1 ExifTool 呼び出し
- 読み取り: `-G:0 -j -a -u -n`（グループ修飾付き、JSON、重複/未知タグ含む、数値変換なし）
- コピー: `-TagsFromFile <入力> -all:all -overwrite_original <出力>`
- 実行ファイル探索順: 明示指定 → アプリバンドル同梱 → `/opt/homebrew/bin` → `/usr/local/bin` → `/usr/bin` → PATH

### 4.2.2 比較から除外する対象
- グループ: `ExifTool`、`File`、`Composite`
- タグ名接頭辞: `ExifTool`、`Warning`、`Error`
- ファイル実体依存: `SourceFile`、`FileName`、`Directory`、`FileSize`、`FileModifyDate`、
  `FileAccessDate`、`FileInodeChangeDate`、`FilePermissions`、`FileTypeExtension`
- TIFF 物理レイアウト（再エンコードで必ず変わる）: `StripOffsets`、`StripByteCounts`、
  `RowsPerStrip`、`TileOffsets`、`TileByteCounts`、`TileWidth`、`TileLength`、
  `Compression`、`PlanarConfiguration`、`Predictor`、`PhotometricInterpretation`、`FillOrder`
- 書き出しツール名: `Software`、`ProcessingSoftware`
- サムネイル再構築: `ThumbnailOffset`、`ThumbnailLength`、`ThumbnailImage`、`PreviewImage`
- ICC（色管理検証で別途扱うため二重に見ない）: `ProfileCMMType`、`ProfileDateTime`、
  `ProfileID`、`ProfileCreator`

### 4.2.3 値の正規化規則
1. Unicode 互換正規化
2. 連続空白（改行・タブ含む）を単一スペースへ畳み、前後を除去
3. 先頭の日付区切りを `YYYY:MM:DD` 表記へ統一（`2026-08-24` → `2026:08:24`）
4. 有理数表記を小数へ評価（`3/1` → `3`、分母 0 はそのまま）
5. 数値として解釈できる場合は表記統一（`1.50` → `1.5`、有効桁 6 桁）

### 4.2.4 判定
- 差分種別: 出力欠落 / 出力側余剰 / 値不一致
- 差分が 0 件のときのみ検証成功。
- 検証失敗時は出力ファイルを削除し、失敗タグを表示する。

## 4.3 処理パイプライン方針（Phase 4 着手前に確定）

### 4.3.1 プレビューは縮小しない
- プレビューも書き出しも **常にフル解像度**（指定テストデータでは 8640 x 4860）で処理する。
- 縮小処理を挟むと見た目が変わるため採用しない。処理時間が長くなることは許容する。
- 結果としてプレビューと最終出力の見え方は原理的に一致する。

### 4.3.2 適用ボタン方式
- スライダーやパラメータの変更では処理を開始しない。
- 「適用」ボタンを押したときだけ処理を開始する。
- 未適用の変更がある間は、その旨を UI で示す。
- `AppState.previewUpdateGeneration` と「最後に適用した generation」の比較で未適用状態を判定する。

### 4.3.3 キャンセル可能・非ブロッキング
- 処理はタイル分割して行い、タイル投入前にキャンセルフラグを確認する。
  `MTLCommandBuffer` は投入後に停止できないため、キャンセル粒度はタイル単位とする。
- 処理は専用のバックグラウンドキューで実行し、メインスレッドは結果受け取りのみ行う。
- 焼き上がったタイルから順にキャンバスへ反映し、完了まで待たせない。
- 進捗をステータスバーに表示する。

### 4.3.4 数値計算上の要件
- 中間バッファは `rgba16Float` を使う。16bit 整数では背景減算で負値が飛び、
  グロー加算で飽和するため。最終的に `rgba16Unorm` へ戻す。
- ガウシアンぼかしは分離可能フィルタ（横 → 縦の 2 パス）で実装する。
- 4 成分 PSF は半径の異なる 4 つのガウシアンの重み付き和とし、
  芯が明るく裾が長い天体写真特有の減衰形状を作る。
- タイル境界の継ぎ目を防ぐため、タイルに半径以上のマージンを持たせて処理し、
  中央部のみを書き戻す。

### 4.3.5 実装した処理内容（Phase 4 で確定）

処理は次の順で行う。すべてリニア RGB、中間は `rgba16Float`。

1. 背景推定: 入力を σ = 背景除去 px / 3 でぼかす（分離可能、横 → 縦）
2. 星抽出: `star = max(入力 - 背景 - ノイズ閾値, 0)`
3. グロー生成: 4 成分ガウシアン PSF
   - σ 倍率: 0.25 / 0.60 / 1.00 / 2.20（芯 → 裾）
   - 重み: 0.55 / 0.25 / 0.13 / 0.07（合計 1.0）
   - 基準 σ = 半径 px / 3。各成分はエネルギー保存。
4. 合成: `ゲイン = 強度 x 不透明度` を掛けて Screen または Add
   - Screen も Add も「不透明度で線形補間」と「グローに不透明度を掛ける」が一致するため、
     シェーダではゲインへ畳み込んでいる（`BlendMath` のテストで等価性を検証）。

各レイヤーは常に**元画像から**星を抽出し、合成先だけが積み上がる。

タイル設計:

- マージン = 背景ぼかし半径 + 最大グロー半径（両者は直列に掛かるので足す）
- タイル寸法 = マージン x 4 を 256 画素単位へ丸め、1024〜2048 に収める
- 実効半径は 3σ（それより外の寄与は 0.3% 未満）

読み書きを分けた ping-pong 構成にしてある。`read_write` テクスチャは Metal の
read-write tier 2 を要求するため、tier 1 の GPU でも動くようにするのが目的。

## 5. フェーズ別実装計画

### Phase 0: 土台構築（1週目）
- 状態: 完了（2026-08-24）
- 進捗:
  - [x] XcodeGen によるプロジェクト構成を作成
  - [x] ドメインモデル初版を実装
  - [x] 最小 UI とアプリ状態管理を実装
  - [x] 単体テスト基盤を追加
- Swift Package / Xcode プロジェクト構成整理
- ドメインモデル定義:
  - Project
  - GlowLayer
  - MaskState
  - ExportJob
- ログ基盤とエラー型（ユーザー向け文言 + 詳細ログ）を実装
- 受け入れ条件:
  - 空プロジェクト起動
  - ダミー状態で UI が描画される

### Phase 1: TIFF 入出力と色管理（1〜2週目）
- 状態: 完了（2026-08-24）
- 進捗:
  - [x] 16bit TIFF の読み込みと基本プロパティ検査
  - [x] 入力 TIFF 台帳（寸法、ビット深度、色情報、SHA256、メタデータ件数）を保持
  - [x] リニア化処理（linearSRGB）を実装
  - [x] 中間 TIFF の書き出しと属性互換チェックを実装
  - [x] Save Panel 経由の中間 TIFF 書き出し導線を実装
  - [x] ICC プロファイル一致の厳密検証（比較仕様を固定して実装）
  - [x] ExifTool 連携を前提にしたメタデータ検証前処理（正規化定義）
  - [x] ExifTool 実行・メタデータコピー・比較判定を実装
  - [x] 最終出力（入力 ICC 復元）の書き出しと検証を実装
- Phase 7 へ引き継ぐ残作業:
  - ExifTool の同梱と署名運用（現状はシステムインストール版を探索して使用）
  - 書き出しパイプライン本体（処理済み画像）との接続
- 16bit TIFF 読み込み、画素情報/ICC/メタデータ台帳を取得
- リニア化処理（入力プロファイルに応じた変換）
- 16bit TIFF 書き出し（中間ファイル出力）
- 受け入れ条件:
  - 16bit TIFF を読み込み可能
  - 書き出し TIFF の寸法/ビット深度/ICC が一致

### Phase 2: キャンバス表示と操作（2週目）
- 状態: 完了（2026-08-24）
- 進捗:
  - [x] MetalKit キャンバス（Fit/50%/100%/200%）
  - [x] パン（ドラッグ、可動域の丸め）、ズーム（Command+スクロール、ピンチ、上下限）
  - [x] 押下中の元画像比較、Option+ドラッグのスプリット比較
  - [x] マスクオーバーレイ表示切替
  - [x] 16bit リニア RGB テクスチャの読み込み（8bit 落ちとガンマ混入を回避）
  - [x] 表示専用の露出調整（書き出しには影響しない）
- MetalKit キャンバス（Fit/100%/任意倍率）
- パン、ズーム、元画像比較（押下中比較 + スプリット比較準備）
- マスクオーバーレイ表示切替
- 受け入れ条件:
  - プレビュー操作が実時間で追従
  - 元画像比較がワンタッチで可能

### Phase 3: レイヤー管理 UI（2〜3週目）
- 状態: 完了（2026-08-24）
- 進捗:
  - [x] レイヤー追加（3プリセット）、複製、削除、表示/非表示、順序変更、名称変更
  - [x] 合成モード（Screen/Add）と不透明度編集
  - [x] インスペクタ（強度、半径、背景除去、ノイズ閾値、空マスク関連）
  - [x] 各パラメータにスライダ・数値表示・既定値へ戻すボタン
  - [x] 半径が推奨上限を超えたときの霞み警告
  - [x] ベース画像を最下段にロック表示
- 残課題:
  - [x] サイドバー幅の可変化（Phase 4 で対応。`HSplitView` へ変更）
- レイヤー追加、複製、削除、表示/非表示、順序変更
- 合成モード（Screen/Add）、不透明度編集
- インスペクタ（強度、半径、背景除去、ノイズ閾値、空マスク関連）
- 受け入れ条件:
  - レイヤー構成が UI で一通り編集できる
  - 値変更でプレビュー更新が走る

### Phase 4: 画像処理パイプライン（3〜5週目）
- 状態: 完了（2026-08-24）
- 進捗:
  - [x] マージン付きタイル分割の処理計画（`GlowProcessingPlan`）
  - [x] 背景減算による星成分抽出
  - [x] 閾値によるノイズ抑制
  - [x] 4 成分ガウシアン PSF でグロー生成（分離可能フィルタ、横 → 縦）
  - [x] Screen/Add でのレイヤー合成（不透明度をゲインへ畳み込み）
  - [x] 「適用」ボタン方式と未適用状態の表示
  - [x] タイル単位のキャンセル、進捗表示、焼き上がったタイルからの段階表示
  - [x] 処理結果を最終 TIFF 書き出しへ接続
  - [x] サイドバー幅の可変化（Phase 3 からの持ち越し）
- 実測（Apple M4 Pro / 8640 x 4860 / 1 レイヤー）:
  - 標準グロー（半径 20px）: 45 タイル / 約 0.2 秒
  - 広いハロー（半径 60px）: 約 0.6 秒
- 受け入れ条件:
  - [x] プレビュー結果と書き出し結果の見え方差異が許容範囲内
    - プレビューと書き出しは同一テクスチャを使うため原理的に一致する。
      テクスチャ → CGImage → TIFF → 読み戻しの往復で画素一致を検証済み。
  - [x] ガンマ空間ぼかしが混入しない（処理は linearSRGB のまま）
- Phase 5 へ引き継ぐ残作業:
  - 空マスクの接続（現状は全面適用。シェーダ側の接続点は用意済み）
  - グローの効き具合の調整（実写では控えめ。強度スライダーの範囲見直しを含む）
  - スプリット比較の境界がテクスチャ座標基準のため、拡大表示時に画面位置とずれる

### Phase 5: 空マスク（自動 + 手動補正）（5〜6週目）
- 状態: 未着手（次に着手）
- 自動空マスク生成
- 地平線指定 UI と境界ぼかし
- 手動ブラシ（加算/除外、ブラシ径/硬さ）
- レイヤーごとに独立したマスク編集
- 受け入れ条件:
  - 前景漏れを視覚的に抑制できる
  - レイヤー別に適用領域を調整できる

### Phase 6: プロジェクト保存（.stellaveil）（6週目）
- 状態: 未着手
- 入力ファイルパス、ハッシュ、レイヤー、マスク、色管理情報を保存
- 再読込とハッシュ不一致検知（再リンク導線）
- 受け入れ条件:
  - 保存→再読込で編集状態が再現
  - 入力差し替え時に警告表示

### Phase 7: メタデータ検証付き書き出し（7〜8週目）
- 状態: 未着手
- ExifTool 同梱と署名運用
- 入力→出力へのメタデータコピー
- 比較対象タグの正規化比較
- 除外対象（ストリップ位置/バイト数等）を定義
- 検証失敗時は保存を中止し、失敗タグを表示
- 受け入れ条件:
  - メタデータ検証成功時のみ最終出力が確定
  - 失敗時の原因表示が可能

## 6. テスト計画

### 6.1 テストデータ
- 指定テストデータ:
  - /Users/junpeiwada/Dropbox/受け渡し用フォルダ/グロー/A1_08098-Mean Max Hor Accuracy.tif

### 6.2 自動テスト
- 実装済み（70 件成功、2026-08-24 時点）:
  - ICC プロファイル名の正規化と `desc` タグ解析（v2/v4、壊れたデータの安全性）
  - 中間出力の linearSRGB 埋め込み、最終出力の入力 ICC 復元、ICC 不一致検出
  - 指定テストデータでの最終書き出し ICC 一致往復
  - メタデータ比較の除外規則と値正規化（過検知しないこと、差分を見落とさないこと）
  - ExifTool 実行（バージョン取得、JSON 読み取り、コピー後の検証成功、意図的差分の検出）
  - キャンバス表示状態（Fit 倍率算出、中央寄せ、パン可動域の丸め、ズーム上下限、
    ビュー寸法未確定時の破綻回避）
  - レイヤー操作（複製時のパラメータ/マスク引き継ぎと ID 再発行、並べ替え、
    範囲外値の丸め、自動判定 nil の保持、プリセットの妥当性）
  - AppState のレイヤー選択遷移（追加時の自動選択、複製後の選択移動、削除時の選択退避）
  - ガウシアン係数（正規化、対称性、3σ 半径）と 4 成分 PSF の重み
  - 合成式（Screen/Add）と、不透明度をゲインへ畳み込む変形の等価性
  - タイル分割（画素の重複・欠落なし、マージンの付き方、タイル寸法の決定）
  - シェーダへ渡すパラメータ構造体のメモリレイアウト
  - Metal 実機でのグロー処理（一様画像は不変、点像がにじむ、進捗通知、キャンセル）
  - タイル分割の結果が単一タイル処理と一致すること（マージン設計の検証）
  - 指定テストデータのフル解像度処理（Screen 合成で暗くならない、背景が一様に持ち上がらない）
  - 処理結果 → CGImage → 最終 TIFF の往復で画素が変化しないこと
  - 未適用の変更がある間は書き出しを行わないこと
- 今後実装:
  - 単体テスト
    - レイヤー合成ロジック（Screen/Add の数式）
    - パラメータ境界値（0%、最大半径、閾値上下限）
    - .stellaveil のエンコード/デコード
  - 結合テスト
    - TIFF 読み込み→プレビュー→書き出しの一連処理
  - 回帰テスト
    - 既知プロジェクトの再読込一致
    - 同一入力での出力差分監視（許容誤差つき）

### 6.3 手動確認
- 強い前景光がある写真でのマスク漏れ確認
- 等倍表示で星のにじみ形状を確認
- プレビューと最終書き出しの見た目一致を確認
- メタデータ検証済み表示とタグ数表示を確認

## 7. リスクと対策
- リスク: メタデータ差分判定の過検知
  - 対策: 除外タグを明示リスト化し、比較正規化を固定
- リスク: 高解像度処理での待ち時間増大
  - 対策: プレビューは縮小処理、書き出しのみフル解像度
- リスク: 自動マスクの誤検出
  - 対策: 手動補正を前提機能として早期に実装

## 8. 完了条件（Definition of Done）
- 16bit TIFF の入力と 16bit TIFF 出力が安定動作する。
- グローレイヤー編集とプレビュー反映が実用速度で動作する。
- 空マスク（自動/手動）で前景漏れを抑制できる。
- .stellaveil 保存/再読込が再現性を満たす。
- メタデータ比較検証が成功した出力のみ保存される。
- 指定テストデータで一連フロー（読み込み→調整→書き出し→検証）を完走できる。
