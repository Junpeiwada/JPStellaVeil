# 技術仕様-JPStellaVeil

## 変更履歴

| 日付 | 変更者 | 変更内容 |
| --- | --- | --- |
| 2026-08-24 | Junpei Wada | `実装計画.md` からアーキテクチャ・色管理・メタデータ比較・処理パイプライン・テスト計画・リスクを分離して初版作成 |

## 目次

- [概要](#概要)
- [1. アーキテクチャ](#1-アーキテクチャ)
- [2. 色管理仕様](#2-色管理仕様)
  - [2.1 ICC の扱い](#21-icc-の扱い)
  - [2.2 ICC 一致判定の比較キー](#22-icc-一致判定の比較キー)
- [3. メタデータ比較仕様](#3-メタデータ比較仕様)
  - [3.1 ExifTool 呼び出し](#31-exiftool-呼び出し)
  - [3.2 比較から除外する対象](#32-比較から除外する対象)
  - [3.3 値の正規化規則](#33-値の正規化規則)
  - [3.4 判定](#34-判定)
- [4. 処理パイプライン](#4-処理パイプライン)
  - [4.1 プレビューは縮小しない](#41-プレビューは縮小しない)
  - [4.2 自動処理（当初は適用ボタン方式）](#42-自動処理当初は適用ボタン方式)
  - [4.3 キャンセル可能・非ブロッキング](#43-キャンセル可能非ブロッキング)
  - [4.4 数値計算上の要件](#44-数値計算上の要件)
  - [4.5 実装した処理内容](#45-実装した処理内容)
  - [4.6 実測値](#46-実測値)
  - [4.7 明るさに対する応答](#47-明るさに対する応答)
  - [4.8 グローのみ表示](#48-グローのみ表示)
  - [4.9 レイヤー別グローの保持](#49-レイヤー別グローの保持)
  - [4.11 明るさ下限と星の分布](#411-明るさ下限と星の分布)
- [4.10 キャンバス描画の座標系](#410-キャンバス描画の座標系)
  - [4.12 広色域入力の扱い（未解決）](#412-広色域入力の扱い未解決)
- [4.12b 中間 TIFF は検証用（UI からは出さない）](#412b-中間-tiff-は検証用ui-からは出さない)
- [4.13 空マスクの生成（Photoshop 連携）](#413-空マスクの生成photoshop-連携)
- [4.14 プリセットの保存形式](#414-プリセットの保存形式)
- [5. テスト計画](#5-テスト計画)
  - [5.1 テストデータ](#51-テストデータ)
  - [5.2 自動テスト（実装済み・131 件成功 / 2026-08-24 時点）](#52-自動テスト実装済み131-件成功--2026-08-24-時点)
  - [5.3 今後実装するテスト](#53-今後実装するテスト)
  - [5.4 手動確認](#54-手動確認)
- [6. リスクと対策](#6-リスクと対策)

---

## 概要

JPStellaVeil の実装で確定した技術仕様をまとめた文書。実装が進むたびに更新する。

- **この文書が扱うもの**: 決定済みの仕様と、その決定理由
- **この文書が扱わないもの**: 進捗・フェーズ管理（[実装計画.md](アーカイブ/実装計画.md) が正本。全フェーズ完了につきアーカイブ済み）
- **関連**: [実装計画.md](アーカイブ/実装計画.md) / [アプリ全体像.md](アプリ全体像.md) / [UI.md](UI.md) / [引き継ぎ.md](アーカイブ/引き継ぎ.md)（アーカイブ済み）

## 1. アーキテクチャ

| 層 | 使用技術 | 担当範囲 |
| --- | --- | --- |
| UI 層 | SwiftUI | レイヤーパネル、インスペクタ、ステータス表示 |
| 描画層 | MetalKit | キャンバス、ズーム、比較表示、マスクオーバーレイ |
| 演算層 | Metal Compute | 星抽出、PSF 畳み込み、合成、マスク適用 |
| IO 層 | ImageIO / ColorSync | 16bit TIFF 入出力、ICC 維持 |
| メタデータ層 | ExifTool（同梱予定） | メタデータのコピーと JSON 比較検証 |
| 永続化層 | Codable JSON | プリセット（`Presets.json`）。§4.14 参照 |

実装上の重要原則:

- 抽出・ぼかし・合成は必ずリニア RGB で実行する。
- プレビューと最終出力の処理契約を一致させる（同一テクスチャを使う。§4.1 参照）。

## 2. 色管理仕様

フェーズ1で確定。

### 2.1 ICC の扱い

- 処理はリニア RGB（`linearSRGB`）で行う。
- 中間 TIFF: `linearSRGB` を埋め込む。入力プロファイルとは一致しないのが正常。
- 最終 TIFF: 処理後に入力 TIFF の ICC プロファイルへ変換して埋め込む。
- 検証は期待値を明示して行う（`ExpectedOutputProfile`）。
  - `.workingSpace(.linearSRGB)`: 中間出力に使用
  - `.matchesInput`: 最終出力に使用

### 2.2 ICC 一致判定の比較キー

- 比較キーはプロファイル記述名の正規化文字列。
- プロファイル名は ICC プロファイルの `desc` タグから取得する。
  - ICC v2 は `desc`（textDescriptionType）、v4 は `mluc`（multiLocalizedUnicodeType）に対応。
  - `CGColorSpace.name` はシステム識別子（`kCGColorSpaceLinearSRGB`）を返し、
    ImageIO の `kCGImagePropertyProfileName` は記述名（`sRGB IEC61966-2.1 Linear`）を返すため、
    両者を突き合わせるには `desc` タグ経由に統一する必要がある。
- 正規化規則: Unicode 互換正規化 → 小文字化 → 英数字以外を除去。
  - 例: `Adobe RGB (1998)` と `adobe-rgb-1998` は同値。
- どちらかがプロファイル名不明の場合は不一致として扱う（保存を止める側に倒す）。
- TIFF の `Model` タグ（カメラ機種名）をプロファイル名の代用にはしない。

## 3. メタデータ比較仕様

フェーズ1で確定。

### 3.1 ExifTool 呼び出し

- 読み取り: `-G:0 -j -a -u -n`（グループ修飾付き、JSON、重複/未知タグ含む、数値変換なし）
- コピー: `-TagsFromFile <入力> -all:all -overwrite_original <出力>`
- 実行ファイル探索順: 明示指定 → アプリバンドル同梱 → `/opt/homebrew/bin` → `/usr/local/bin` → `/usr/bin` → PATH

### 3.2 比較から除外する対象

- グループ: `ExifTool`、`File`、`Composite`
- タグ名接頭辞: `ExifTool`、`Warning`、`Error`
- ファイル実体依存: `SourceFile`、`FileName`、`Directory`、`FileSize`、`FileModifyDate`、
  `FileAccessDate`、`FileInodeChangeDate`、`FilePermissions`、`FileTypeExtension`
- TIFF 物理レイアウト（再エンコードで必ず変わる）: `StripOffsets`、`StripByteCounts`、
  `RowsPerStrip`、`TileOffsets`、`TileByteCounts`、`TileWidth`、`TileLength`、
  `Compression`、`PlanarConfiguration`、`Predictor`、`PhotometricInterpretation`、`FillOrder`
- 書き出しツール名: `Software`、`ProcessingSoftware`
- サムネイル再構築: `ThumbnailOffset`、`ThumbnailLength`、`ThumbnailImage`、`PreviewImage`
- ICC（色管理検証で別途扱うため二重に見ない）: `ProfileCMMType`、`ProfileDateTime`、
  `ProfileID`、`ProfileCreator`

### 3.3 値の正規化規則

1. Unicode 互換正規化
2. 連続空白（改行・タブ含む）を単一スペースへ畳み、前後を除去
3. 先頭の日付区切りを `YYYY:MM:DD` 表記へ統一（`2026-08-24` → `2026:08:24`）
4. 有理数表記を小数へ評価（`3/1` → `3`、分母 0 はそのまま）
5. 数値として解釈できる場合は表記統一（`1.50` → `1.5`、有効桁 6 桁）

### 3.4 判定

- 差分種別: 出力欠落 / 出力側余剰 / 値不一致
- 差分が 0 件のときのみ検証成功。
- 検証失敗時は出力ファイルを削除し、失敗タグを表示する。

## 4. 処理パイプライン

フェーズ4で確定。

### 4.1 プレビューは縮小しない

- プレビューも書き出しも **常にフル解像度**（指定テストデータでは 8640 x 4860）で処理する。
- 縮小処理を挟むと見た目が変わるため採用しない。処理時間が長くなることは許容する。
- 結果としてプレビューと最終出力の見え方は原理的に一致する。

### 4.2 自動処理（当初は適用ボタン方式）

**当初は「適用ボタンを押したときだけ処理する」方式だったが、フェーズ4完了後に自動処理へ変更した。**
フル解像度でも標準設定なら 0.2 秒程度と十分速いことが実測で分かったため。

- パラメータ変更、レイヤーの表示切替、表示モード変更で自動的に処理を開始する。
- スライダーのドラッグ中に処理を投げ続けないよう 150ms のデバウンスを挟む
  （`AppState.autoApplyDelay`）。
- 実行中に新しい要求が来たら、古い処理をキャンセルして最新の値で処理し直す。
- 「中止」で止めるのは**いま走っている処理だけ**。次に値を変えれば自動で処理し直す。
  当初は中止で自動処理そのものを止めていたが、
  以降どのパラメータを動かしても何も起きず「効かない」状態に見えるため取りやめた。
  中止直後は「未処理の変更があります」と表示し、そのままやり直せるボタンを出す。
- `AppState.previewUpdateGeneration` と「最後に適用した generation」の比較で未適用状態を判定する。

### 4.3 キャンセル可能・非ブロッキング

- 処理はタイル分割して行い、タイル投入前にキャンセルフラグを確認する。
  `MTLCommandBuffer` は投入後に停止できないため、キャンセル粒度はタイル単位とする。
- 処理は専用のバックグラウンドキューで実行し、メインスレッドは結果受け取りのみ行う。
- 焼き上がったタイルから順にキャンバスへ反映し、完了まで待たせない。
- 進捗をステータスバーに表示する。

### 4.4 数値計算上の要件

- 中間バッファは `rgba16Float` を使う。16bit 整数では背景減算で負値が飛び、
  グロー加算で飽和するため。最終的に `rgba16Unorm` へ戻す。
- ガウシアンぼかしは分離可能フィルタ（横 → 縦の 2 パス）で実装する。
- 4 成分 PSF は半径の異なる 4 つのガウシアンの重み付き和とし、
  芯が明るく裾が長い天体写真特有の減衰形状を作る。
- タイル境界の継ぎ目を防ぐため、タイルに半径以上のマージンを持たせて処理し、
  中央部のみを書き戻す。

### 4.5 実装した処理内容

処理は次の順で行う。すべてリニア RGB、中間は `rgba16Float`。

1. 背景推定: 入力を σ = 背景除去 px / 3 でぼかす（分離可能、横 → 縦）
2. 星抽出: `star = max(入力 - 背景, 0)`
2.5. 明るさ下限を**星単位**で適用する。
   星成分を σ 1.5 でぼかした値（近傍のピーク）で判定し、
   `star * smoothstep(下限, 下限 x 1.08, ピーク)` とする。
   画素ごとの明るさで判定すると、下限を上げるほど星が外周から削られて痩せ、
   グロー自体が小さく暗くなってしまう。
3. グロー生成: 4 成分ガウシアン PSF
   - σ 倍率: 0.25 / 0.60 / 1.00 / 2.20（芯 → 裾）
   - 重み: 0.55 / 0.25 / 0.13 / 0.07（合計 1.0）
   - 明るさしきい値係数: 0.00 / 0.05 / 0.15 / 0.35（芯 → 裾）
   - 基準 σ = 半径 px / 3。各成分はエネルギー保存。
   - 明るさしきい値係数: 0.00 / 0.03 / 0.10 / 0.25（芯 → 裾）
   - 明るさ応答による重みの増減率: 0.00 / 0.50 / 2.00 / 5.00（芯 → 裾）
   - 各成分は畳み込み前に「明るさ応答 x しきい値係数」でゲートを掛ける（4.7 節）。
4. 合成: `ゲイン = 強度 x 不透明度` を掛けて Screen または Add
   - Screen も Add も「不透明度で線形補間」と「グローに不透明度を掛ける」が一致するため、
     シェーダではゲインへ畳み込んでいる（`BlendMath` のテストで等価性を検証）。

各レイヤーは常に**元画像から**星を抽出し、合成先だけが積み上がる。

タイル設計:

- マージン = 背景ぼかし半径 + ピーク検出半径 + 最大グロー半径（直列に掛かるので足す）
- タイル寸法 = マージン x 4 を 256 画素単位へ丸め、2048〜3072 に収める
- 1 つのコマンドバッファへ 4 タイルまで詰める（同期回数を減らすため）。
  キャンセルの粒度はこのバッチ単位になる
- 実効半径は 3σ（それより外の寄与は 0.3% 未満）

読み書きを分けた ping-pong 構成にしてある。
これは当初 read-write tier 1 の GPU でも動かすための配慮だったが、
対象を Apple Silicon に絞ったため制約としては不要になった（構成はそのまま維持している）。

### 4.6 実測値

Apple M4 Pro / 8640 x 4860 / 1 レイヤー:

| 条件 | タイル数 | 所要時間 |
| --- | --- | --- |
| 標準グロー（半径 20px） | 15 | 約 0.16 秒 |
| 広いハロー（半径 60px） | — | 約 0.5 秒 |

当初はタイル寸法 1024（45 タイル）で約 0.2 秒だった。
タイルを大きくするとマージンの無駄（23% → 11%）と CPU-GPU 同期回数（45 → 4）が減る。

### 4.7 明るさに対する応答

畳み込みは線形なので、しきい値が無いと星の明るさが変わっても PSF の**形**は変わらず、
振幅が比例するだけになる。ガウシアンの裾は `exp(-r²/2σ²)` なので、
視認できる半径は `r ∝ √(ln B)` でしか増えない。
**明るさが 10 倍でもハロー半径は 1.5 倍程度**にしかならず、
明るい星と暗い星でハローの大きさに差が出ない。

実写では明るい星ほど目に見えて大きなハローを持つため、
成分ごとに明るさしきい値を設けて **PSF の形自体を明るさで変える**方式を採った。

- 明るさ下限も成分ごとのしきい値も、同じゲート関数で扱う。
  引き算にすると残った星まで下限のぶん暗くなるため（実測では下限 0.5 で最大値が 70% 減）。
- 明るさ下限の判定には近傍のピークを使う（星単位の選別にするため）。
  この修正で、下限 0.2 まで最も明るいグローが変わらなくなった（0.3926 → 0.3923）。
- 成分 i の入力は、しきい値付近で滑らかに立ち上がるゲートを掛けた値。
  `smoothstep(t, t * 1.08, star) * star`（t = 明るさ応答 x しきい値係数_i）。
  当初は `max(star - t, 0)` と引き算していたが、振幅まで落ちて明るい星の裾が痩せ、
  効果がほとんど分からなかったためゲート方式へ変えた。
- 広い成分ほど係数が大きいので、暗い星は芯だけ、明るい星は裾まで乗る。
- 「明るさ応答」（0〜1、既定 0.5）で、しきい値と重みの両方を動かす。
- 0 のときはしきい値が 0、重みも既定のままで、どの星も同じ形になる。

しきい値は畳み込みの**横パスの読み込み時**に適用する（`blurHorizontal`）。
追加のパスが要らないので、成分数を増やしても処理時間は変わらない。

**しきい値だけでは足りなかった。** 暗い星のグローが減るだけで、
肝心の「明るい星ほど大きなハロー」にならないため、重みも動かしている。
明るさ応答 1 のときの重み合計は星の明るさで変わる:

| 星の明るさ | 乗る成分 | 重み合計 |
| --- | --- | --- |
| 暗い（〜0.03） | 芯のみ | 0.55 |
| 中くらい（0.03〜0.10） | + 内側 | 0.925 |
| 明るい（0.10〜0.25） | + 主ハロー | 1.315 |
| 非常に明るい（0.25〜） | + 裾 | 1.735 |

芯の重みは動かさない。減らすと明るい星のピークまで落ちて逆に目立たなくなる
（実測で試したところ、ピークが 0.100 から 0.061 まで落ちた）。

合成画像（明るい星 0.8 / 暗い星 0.01、半径 30px）での実測:

| 明るさ応答 | 明るい星の総量 | 暗い星の総量 |
| --- | --- | --- |
| 0.0 | 0.332 | 0.0041 |
| 1.0 | 0.417（+26%） | 0.0032（-22%） |

### 4.8 グローのみ表示

追加されるグロー成分だけを確認するための表示モード（`GlowOutputMode.glowOnly`）。

- 原画を含めず、すべてのレイヤーを加算合成する。
- Screen 指定のレイヤーもこのモードでは加算になる（成分そのものを見るのが目的のため）。
- レイヤー別グローを保持しているので、切り替えても再処理は要らない（4.9 節）。

### 4.9 レイヤー別グローの保持

グローは「畳み込み → ゲイン → 合成」の順に処理される。
**後段はすべて線形**なので、畳み込み結果さえ保持しておけば描画時に適用できる。

- 各レイヤーのグローを **ゲイン適用前**の `rgba16Float` フル解像度テクスチャで保持する
- 表示はキャンバスのフラグメントシェーダが毎フレーム合成する
- 書き出しは `compositeLayers` カーネルが同じ式で合成する（見え方が一致する）

再処理が要るのは `GlowConvolutionKey` の値が変わったときだけ。

| 変更 | 扱い |
| --- | --- |
| 半径、背景除去、明るさ下限、明るさ応答 | 畳み込みからやり直す |
| 強度、不透明度、合成モード | 描画時に適用（再処理なし） |
| 表示/非表示、並べ替え、グローのみ表示 | 描画時に適用（再処理なし） |
| レイヤー追加 | 追加分だけ処理する（既存は再利用） |

非表示のレイヤーも畳み込んでおく。表示切替を再処理なしで即座に反映するため。
同時に扱えるレイヤーは 8 枚（シェーダのテクスチャ配列サイズ）。

VRAM の内訳（8640 x 4860 = 42M 画素、16bit x 4 チャンネルで 1 枚 336MB）:

| 用途 | フォーマット | サイズ |
| --- | --- | --- |
| 原画 | rgba16Unorm | 336 MB |
| レイヤー別グロー | rgba16Float | 336 MB / レイヤー |
| タイル中間バッファ 4 枚 | rgba16Float・2160² | 149 MB（処理中のみ） |
| 書き出し時の合成先 | rgba16Unorm | 336 MB（書き出し時のみ） |
| ドロウアブル | bgra8Unorm・画面サイズ | 約 44 MB |

**表示そのものは画面解像度の 8bit なので軽い。** 容量を使うのは画像データ本体である。

### 4.11 明るさ下限と星の分布

`明るさ下限`（旧「明るさ下限」）は、背景減算後の星成分から一律に引く下限値。

- 小さい値（0.001〜0.01）は高 ISO ノイズの抑制に効く
- 大きい値（0.1〜1.0）は暗い星を切り捨て、明るい星だけにグローをかける

指定テストデータでの実測（背景減算後の星成分、間引きサンプル 1050 万画素）:

| 分位 | 明るさ |
| --- | --- |
| 中央値 | 0.0001 |
| 上位 1% | 0.033 |
| 上位 0.1% | 0.232 |
| 上位 0.01% | 0.720 |
| 最大 | 0.875 |

| しきい値 | 残る画素 |
| --- | --- |
| 0.004（既定） | 11.6% |
| 0.01 | 2.7% |
| 0.05 | 0.73% |
| 0.1 | 0.37% |
| 0.2 | 0.13% |
| 0.4 | 0.03% |

「明るい星だけ」に絞るには 0.2 以上が要るため、範囲は 0〜1.0 とした。
既定値 0.004 は線形スライダーでは左端に潰れるので、UI は対数目盛りにしている。

分布は `GlowPipeline.measureStarHistogram` が GPU で集計する（背景減算までで止める）。
明るさ下限を動かしても測り直す必要はなく、背景除去が変わったときだけ再計算する。
インスペクタにはグロー対象の割合（%）だけを表示する。

一度は分布をヒストグラムとして描画したが取りやめた。
**横軸のリニア値が写真上のどの星に当たるか分からず、判断材料にならなかったため。**
どの星が対象になるかは「グローのみ表示」で明るさ下限を動かして確かめるのが実際的である。

## 4.10 キャンバス描画の座標系

拡大縮小とパンは **テクスチャ座標で表現する**。ビューポートは描画領域全体に固定する。

当初はビューポートを画像の描画矩形に合わせていたが、
8640px の画像を等倍表示すると Retina で 17280px となり、
Metal のビューポート上限（16384）を超えて描画が破綻していた。
パンで原点が動くと歪み方が変わるため「2 本指ドラッグでアスペクト比が変わる」症状になる。

- `CanvasViewState.textureMapping` が画面座標（左上原点 0〜1）から
  テクスチャ座標への変換係数を返す
- シェーダは `uv = screenCoord * scale + offset` で変換し、範囲外は背景色で塗る
- スプリット比較の境界も画面座標で判定するため、拡大してもマウス位置と一致する

アスペクト比が倍率によらず保たれることは `CanvasViewStateTests` で検証している。

### 4.12 広色域入力の扱い（未解決）

処理は `linearSRGB` で行う設計だが、指定テストデータは ProPhoto RGB（ROMM RGB）で
それより色域が広い。取り込みの時点で色域外がクリップされる。

前景（人工光）を含む 512x512 の領域で往復（ProPhoto → linearSRGB → ProPhoto）を測った結果:

| 指標 | 値 |
| --- | --- |
| 最大差 | 0.047（8bit の約 12 階調） |
| 平均差 | 0.0016 |
| 0.002 を超えて変わった成分 | 18.3% |

星空の暗部では問題にならないが、**彩度の高い人工光では目に見える差が出る**。
`CGColorSpace.linearized()` で入力の色域を保ったままリニア化すれば避けられるが、
色管理仕様（2 章）の変更になるため未着手。`WideGamutRoundTripTests` が現状値を記録している。

## 4.12b 中間 TIFF は検証用（UI からは出さない）

書き出しには 2 つの経路があるが、利用者が使うのは最終 TIFF だけである。

| | 中間 TIFF | 最終 TIFF |
| --- | --- | --- |
| グロー・空マスク | **含まない**（入力そのまま） | 適用する |
| ICC | linearSRGB を埋め込む | 入力と同じ ICC へ戻す |
| メタデータ | コピーしない | ExifTool でコピーし検証する |
| 検証失敗時 | — | 出力を削除して中止 |

中間 TIFF は「入力を linearSRGB へ変換して書き出す」だけのもので、
処理はリニア空間で行うという方針（2 章）が正しく働くかを確かめるために用意した。
選ぶと意図しない出力になるため、メニューからは外してある
（`AppState.exportIntermediate` と検証テストは残している）。

## 4.13 空マスクの生成（Photoshop 連携）

自動空マスクは自前で判定せず、**Photoshop の「空を選択」（`selectSky`）を借りる**。
輝度しきい値や地平線指定といった自前の方法より、
星景写真（天の川 + 明るい前景）での分離精度が明らかに高いため。

- `Scripts/sky_mask.jsx` をアプリバンドルへ同梱し、osascript 経由で Photoshop に実行させる
- 出力は 16bit グレースケール TIFF（白 = 空 = グローを乗せる）
- 入力ファイルは開かれるだけで変更されない（`DONOTSAVECHANGES` で閉じる）
- Photoshop が無い環境では機能を無効化し、理由を表示する

実測（8640 x 5760 / ProPhoto RGB / 398MB）:

| 条件 | 所要時間 | 出力サイズ |
| --- | --- | --- |
| フル解像度・Photoshop 起動済み | 約 3 秒 | 3.8 MB |
| フル解像度・Photoshop 未起動 | 約 14 秒 | 3.8 MB |

マスクは滑らかなので LZW がよく効き、フル解像度でも軽い。縮小する理由がない。

マスクの適用は**畳み込みの後**に掛けるだけでよいので、
表示側（`canvasFragment`）と書き出し側（`compositeLayers`）の合成で適用している。
マスクを付け外ししても畳み込みのやり直しは要らない。

テクスチャは `r16Unorm`（1 成分）。8640 x 5760 で 100MB。RGBA で持つと 400MB になる。

マスクは生成後も保持し、有効・無効をトグルで切り替えられる（作り直さずに効果を比べられる）。

実装しないと決めたもの（2026-08-25）:

- **レイヤーごとの独立したマスク**: 全レイヤーで共通の 1 枚とする（UI.md の記述より、こちらが現行仕様）
- **手動ブラシによる補正**
- **地平線指定と境界ぼかし**

Photoshop の「空を選択」で実用精度が出ており、修正が要るときも Photoshop 側で直すほうが早いため。
星景写真の空と地上の境界（木立・稜線・電線）はヒューリスティックが最も苦手とする形状で、
同等をアプリ単体でやるならセグメンテーションモデルの搭載が必要になる。
`SkyMaskState` の `horizonY` / `featherRadius` はドメインモデルに残っているが、処理へは繋がっていない。

## 4.14 プリセットの保存形式

グローの設定は**レイヤー構成全体**を1件として保存する。星景の仕上がりは「細いグロー + 広いハロー」のような重ね方で決まるため、1枚だけ保存しても再現できない。

保存先は `~/Library/Application Support/JPStellaVeil/Presets.json` の1ファイル。1件1ファイルにしないのは、ユーザーが決めた並び順を素直に保てるからである。

```json
{
  "schemaVersion": 1,
  "presets": [
    {
      "id": "…UUID…",
      "name": "夏の天の川",
      "updatedAt": "2026-08-25T03:00:00Z",
      "layers": [
        {
          "name": "微細グロー",
          "isVisible": true,
          "blendMode": "screen",
          "opacity": 0.6,
          "glow": { "intensity": 1.2, "radius": 8, "brightnessResponse": 0.3 },
          "extraction": { "backgroundRemoval": 8, "brightnessFloor": 0.006 }
        }
      ]
    }
  ]
}
```

- `layers` は下から順（末尾が最前面）。`StellaVeilProject.layers` と同じ並び。
- レイヤーの `id` と `skyMask` は保存しない。ID は適用のたびに振り直し、空マスクは写真ごとに違うので持ち回らない。
- `updatedAt` は上書き保存のときだけ動かす。適用しただけで動くと管理画面の並びが意味を失う。
- 組み込み3種は JSON には入れず、コード側に固定 UUID (`GlowPreset.presetID`) を持たせて一覧へ混ぜる。起動のたびに ID を発行すると `appliedPresetID` の参照が切れるため。

読み込みで壊れたファイルに当たったときは `Presets-broken-<日時>.json` へ退避してから空で起動する。上書きして中身を失わないようにするため。`schemaVersion` が未知（将来のバージョンが書いた）の場合は退避も上書きもせず、読み取り専用として保存自体を止める。

管理画面からは1件単位で `.jpsvpreset`（中身は `GlowPresetRecord` の JSON）として書き出し・読み込みができる。読み込み時は ID を振り直し、名前が重なる場合は `夏の天の川 2` のようにずらす。同じファイルを2回読んで片方が消えるより、2件並ぶほうが分かりやすいため。

## 5. テスト計画

### 5.1 テストデータ

指定テストデータ:

```
/Volumes/RAID1-8T4/Storage2025/2026/2026-08-12/A1_04276-Mean Max Hor Accuracy.tif
```

### 5.2 自動テスト（実装済み・131 件成功 / 2026-08-24 時点）

- ICC プロファイル名の正規化と `desc` タグ解析（v2/v4、壊れたデータの安全性）
- 中間出力の linearSRGB 埋め込み、最終出力の入力 ICC 復元、ICC 不一致検出
- 指定テストデータでの最終書き出し ICC 一致往復
- メタデータ比較の除外規則と値正規化（過検知しないこと、差分を見落とさないこと）
- ExifTool 実行（バージョン取得、JSON 読み取り、コピー後の検証成功、意図的差分の検出）
- キャンバス表示状態（Fit 倍率算出、中央寄せ、パン可動域の丸め、ズーム上下限、
  ビュー寸法未確定時の破綻回避）
- レイヤー操作（複製時のパラメータ/マスク引き継ぎと ID 再発行、並べ替え、
  範囲外値の丸め、自動判定 nil の保持、プリセットの妥当性）
- AppState のレイヤー選択遷移（追加時の自動選択、複製後の選択移動、削除時の選択退避）
- ガウシアン係数（正規化、対称性、3σ 半径）と 4 成分 PSF の重み
- 合成式（Screen/Add）と、不透明度をゲインへ畳み込む変形の等価性
- タイル分割（画素の重複・欠落なし、マージンの付き方、タイル寸法の決定）
- シェーダへ渡すパラメータ構造体のメモリレイアウト
- Metal 実機でのグロー処理（一様画像は不変、点像がにじむ、進捗通知、キャンセル）
- タイル分割の結果が単一タイル処理と一致すること（マージン設計の検証）
- 指定テストデータのフル解像度処理（Screen 合成で暗くならない、背景が一様に持ち上がらない）
- 処理結果 → CGImage → 最終 TIFF の往復で画素が変化しないこと
- 未適用の変更がある間は書き出しを行わないこと
- 明るさしきい値の並び（芯は 0、裾ほど高い）と明るさ応答によるスケール
- 明るさ応答を上げたとき、暗い星の方が明るい星より強く削られること
- グローのみ表示に原画が含まれないこと、レイヤーが無ければ黒になること
- 中止で自動処理が止まり、手動の適用で再開すること
- 畳み込みに影響しない変更（強度、不透明度、合成モード、表示切替）で再処理を要求しないこと
- 保持したグローを使い回し、ゲインだけ変えて合成し直せること
- 非表示レイヤーが合成に含まれないこと、レイヤー数の上限を超えたら弾くこと

### 5.3 今後実装するテスト

- 単体テスト
  - パラメータ境界値（0%、最大半径、閾値上下限）
- 結合テスト
  - TIFF 読み込み → 調整 → 書き出し → メタデータ検証の一連処理（実装計画 P7-4）
- 回帰テスト
  - 同一入力での出力差分監視（許容誤差つき）

### 5.4 手動確認

- 強い前景光がある写真でのマスク漏れ確認
- 等倍表示で星のにじみ形状を確認
- プレビューと最終書き出しの見た目一致を確認
- メタデータ検証済み表示とタグ数表示を確認
- 各フェーズ完了時にアプリを起動し、スクリーンショットで確認してからコミットする

## 6. リスクと対策

| リスク | 対策 |
| --- | --- |
| メタデータ差分判定の過検知 | 除外タグを明示リスト化し、比較正規化を固定（§3.2 / §3.3） |
| 高解像度処理での待ち時間増大 | タイル分割による段階表示とキャンセルで待ちを隠す。縮小プレビューは見た目が変わるため採用しない（§4.1 / §4.3） |
| 自動マスクの誤検出 | 手動補正を前提機能として同一フェーズで実装する（フェーズ5） |
| Metal の read-write tier 1 GPU で動かない | ping-pong 構成で `read_write` テクスチャを使わない（§4.5） |
